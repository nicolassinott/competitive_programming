#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main() {
    ll s, p, m, n;
    cin >> s >> p >> m >> n;

    vector<ll> trips(n);

    for(int i = 0; i < n; i++) cin >> trips[i];

    

    return 0;
}