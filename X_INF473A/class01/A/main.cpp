#include <bits/stdc++.h>
using namespace std;

int main(){
    int to_print;

    while(1){
        cin >> to_print;
        if(to_print != 42) cout << to_print << endl;
        else break;
    }

    return 0;
}